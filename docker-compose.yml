services:
  # Optional: self-hosted libSQL server (sqld)
  # Uncomment to use instead of embedded mode
  # sqld:
  #   image: ghcr.io/tursodatabase/libsql-server:latest
  #   ports:
  #     - "8081:8080"
  #   volumes:
  #     - sqld-data:/var/lib/sqld
  #   environment:
  #     - SQLD_NODE=primary

volumes:
  # sqld-data:
